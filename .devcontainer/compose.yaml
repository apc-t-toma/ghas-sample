services:
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    env_file:
      - .env_devcontainer
    volumes:
      - ..:/home/vscode/ghas-sample
    command: sleep infinity
